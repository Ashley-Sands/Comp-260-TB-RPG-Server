# the functions used to enforce the message types
# every think must contain a from client name

# See Protocol list for details


def message( from_client_name, to_clients, message ):
    return locals()